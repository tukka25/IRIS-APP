name: Build & Release
permissions:
  contents: write

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      variant:
        description: 'Build variant'
        required: false
        default: 'debug'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: 17
          distribution: temurin
          cache: gradle

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/native
          key: gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
          restore-keys: gradle-

      - name: Build debug APK
        run: ./gradlew assembleDebug
        if: github.event.inputs.variant != 'release'

      - name: Build release APK
        run: ./gradlew assembleRelease
        if: github.event.inputs.variant == 'release' || startsWith(github.ref, 'refs/tags/v')

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: iris-debug-apk
          path: app/build/outputs/apk/debug/*.apk
        if: github.event.inputs.variant != 'release'

      - name: Upload release APK
        uses: actions/upload-artifact@v4
        with:
          name: iris-release-apk
          path: app/build/outputs/apk/release/*.apk
        if: github.event.inputs.variant == 'release' || startsWith(github.ref, 'refs/tags/v')

      - name: Create Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          files: app/build/outputs/apk/release/*.apk
          generate_release_notes: true
          draft: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # ─── Optional: signed release build ────────────────────────────
  # Uncomment and add secrets KEYSTORE_BASE64, KEYSTORE_PASSWORD,
  # KEY_ALIAS, KEY_PASSWORD to enable signed release APKs.
  # sign-release:
  #   runs-on: ubuntu-latest
  #   needs: build
  #   steps:
  #     - uses: actions/checkout@v4
  #     - uses: actions/setup-java@v4
  #       with:
  #         java-version: 17
  #         distribution: temurin
  #         cache: gradle
  #     - uses: android-actions/setup-android@v3
  #     - name: Decode keystore
  #       env:
  #         KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
  #       run: echo "$KEYSTORE_BASE64" | base64 -d > app/release.keystore
  #     - name: Build signed release
  #       run: ./gradlew assembleRelease
  #       env:
  #         KEYSTORE_FILE: app/release.keystore
  #         KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
  #         KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
  #         KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
  #     - name: Upload signed APK
  #       uses: actions/upload-artifact@v4
  #       with:
  #         name: iris-release-signed-apk
  #         path: app/build/outputs/apk/release/*.apk